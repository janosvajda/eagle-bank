NODE_ENV=preprod
PORT=3000
JWT_EXPIRES_IN=1h
AWS_REGION=eu-west-2
# CDK constructs DATABASE_URL from RDS coordinates and the SSM database password.
# ECS injects user, Auth-service, and Ledger-service JWT keys from Parameter Store.
